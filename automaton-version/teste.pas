program teste;
var
  idade: integer;
  salario: real;
begin
  idade := 25;
  salario := 1200.0;

  if (idade >= 18) and (salario >= 1000.0) then
  else

  if (idade >= 65) or ((idade >= 60) and (salario >= 2000.0)) then
  else
end.